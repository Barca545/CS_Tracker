from pandas import DataFrame as df
from pandas import concat
import api_calls

def delta_CS():
    cs_graph_dict = {}
    #have the slope = the change in cs between each frame. 
    #Could probably use a for/while loop
    #ignore the time from 0 to 1

    #delta_cspm = current_cs - last_cs
    #cs_slope = 
    #cs_graph_dict[time] = 
    #cs_graph = df(cs_graph_dict,columns=['Time','ΔCSPM']) 
    print(cs_graph_dict)

delta_CS()
